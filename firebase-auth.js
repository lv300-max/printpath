/**
 * firebase-auth.js — CONFIG REFERENCE (no longer used for module loading)
 *
 * Auth is now handled inline in Print Path Web Demo.html
 * using the Firebase compat SDK (firebase-app-compat.js + firebase-auth-compat.js).
 *
 * TO CONFIGURE:
 * 1. Go to https://console.firebase.google.com
 * 2. Create project → Add web app (</>)
 * 3. Copy the firebaseConfig object
 * 4. Open "Print Path Web Demo.html"
 * 5. Find the section:  // ── PASTE YOUR FIREBASE CONFIG HERE ──
 * 6. Replace the placeholder values with your real config
 *
 * FIREBASE CONSOLE SETUP:
 * - Authentication → Sign-in method → Google → Enable
 * - Authentication → Settings → Authorized domains → add: printpath-ai.netlify.app
 */
